import {StringInput} from "./components/Inputs/StringInput.tsx";

export const App = () => {
    return (
        <StringInput
            placeholder='Имя'
            textHint='хоть какая-то поммощь'/>
    );
}